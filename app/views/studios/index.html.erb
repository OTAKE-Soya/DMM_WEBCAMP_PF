<div class="container">
  <div class="row">
    <h2>スタジオ一覧</h2>
  </div>
  <div class="row">
    <div>
      <% @studios.each do |studio| %>
        <%= link_to studio.name, studio_path(studio.id) %>
      <% end %>
    </div>
  </div>
</div>